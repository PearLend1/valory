/**
 * Shared popularity signals module.
 * Re-exports from the root-level popularity-signals module so
 * both server code and client code can import from '@shared/popularity-signals'.
 */
export * from '../popularity-signals';
